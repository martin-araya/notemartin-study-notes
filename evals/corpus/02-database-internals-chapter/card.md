# 02 — "Database Internals" sample chapter (scanned variant)

## Identificador
`02-database-internals-chapter`

## Título y autor / fuente
"Database Internals: A deep dive into how distributed data systems work" — Alex Petrov (O'Reilly Media, 2019). Sample chapter (Chapter 1: "Introduction and Overview"). **Variante escaneada**: para satisfacer la cobertura de "PDF escaneado con OCR sucio" como fuente friendly (no hostile), se elige una versión escaneada del sample chapter.

## URL canónica
https://www.oreilly.com/library/view/database-internals/9781492040330/ (versión nativa, referencia).

**Muestra escaneada:** Internet Archive — sustituto concreto `formallanguagede0000tbst` (1966, "Formal Language Description Languages for Computer Programming") o equivalente en dominio público. Si no accesible, la muestra queda pendiente.

## Licencia
Copyright O'Reilly Media para la versión nativa. **Para evaluación**: la muestra escaneada usada debe ser dominio público o CC-BY; O'Reilly no es redistribuible. La categoría "PDF escaneado con OCR sucio" se cubre con una fuente equivalente en dominio público si O'Reilly no es accesible.

## Formato
**PDF escaneado con OCR sucio** (PDF generado a partir de imágenes; sin capa de texto nativa; OCR requerido y limpio esperado).

## Páginas
~30 (capítulo de muestra).

## Densidad
- Tablas: baja.
- Código: media (pseudocódigo, fragmentos SQL).
- Figuras: alta (diagramas de arquitectura).
- Fórmulas: baja.

## Idioma
inglés.

## Versión del producto
no aplica.

## Presencia
- `tablas`: sí (pocas)
- `codigo`: sí
- `formulas`: no
- `figuras`: sí
- `diagramas_sintaxis`: no
- `cajas_editoriales`: sí (Note, Tip)

## Hostilidad
`none`.

## OCR requerido
**`sí`** — confianza media esperada entre 0.65 y 0.85; reintento ≤ 2; si < 0.70 tras reintentos, se bloquea por umbral (`architecture.md` §8).

## Destinos esperados
Todos (Obsidian, Notion API, Notion import, AppFlowy, Markdown, HTML/PDF, Flashcards).

## Tipos de nota esperados
- `concept` (conceptos de almacenamiento distribuido).
- `comparison` (comparación de motores).
- `cheatsheet` (resumen del capítulo).
- `architecture` (diagramas de arquitectura).

## Plan de ingesta
L0 nativo (PDF con capa de texto); L1 SDM; L2 unidades `definition`, `mechanism`; L3 redactar como `concept` y `architecture`; L4 a todos los destinos.

## Categoría cubierta
Capítulo de libro técnico de editorial **+ PDF escaneado con OCR sucio** (versión friendly; la versión hostile cubre el caso más degradado en #13).

## Categoría adicional cubierta
Documento con figuras de arquitectura (diagramas).

## Riesgo si falta
Sin esta fuente no hay cobertura para "libro técnico de editorial"; el agente no练习ía la cadena sobre prosa explicativa larga con diagramas.

## Muestra
- Archivo: `sample.pdf` — **no descargado en esta fase**.
- Motivo: la muestra pública de O'Reilly requiere registro; no se descarga material con acceso restringido sin credenciales.
- Sustituto propuesto: `Foundations of Databases` (Abiteboul, Hull, Vianu), PDF disponible libremente. La descarga queda pendiente de T3-bis (cuando un eval real lo necesite).
- Hash sha256: pendiente.
